---
"prescriptive": patch
---

Document `@prescriptive/reativa` in the root README: add it to the packages
table and the repository layout, note that its Melange build needs an OCaml
toolchain and so runs separately from `npm run build:packages`, and mention that
the website's live examples can be switched between the Xote and reativa
implementations.
